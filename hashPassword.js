// const bcrypt = require("bcrypt");

// const hashPassword = async (password) => {
//   const result = await bcrypt.hash(password, 10); //кешує пароль
//   console.log(result);
//   const result1 = await bcrypt.compare(password, result); //перевіряє чи є другий елемент закешована версія першого
//   console.log(result1);
// };

// hashPassword("12345678");
